

const SectionTitle = ({title}) => {
    return (
       <>
            <h2 className="text-center uppercase font-semibold border-y-slate-500 py-3 md:text-xl  lg:text-3xl">{title}</h2>
       </>
    );
};

export default SectionTitle;